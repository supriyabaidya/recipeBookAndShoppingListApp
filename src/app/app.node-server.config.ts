// configuration of node server(backend to this frontend)
// serverHost = hostname where node is hosted
// serverPort = port where node is hosted

// Tip: port 443 is the port which is used for https (secured)

export let config = {
  // serverHost: 'http://localhost',
  serverHost: 'https://my-node-mongodb-test.193b.starter-ca-central-1.openshiftapps.com',
  serverPort: 443
};
